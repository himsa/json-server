{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "feed.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server feed.js --id feedId db.json --routes routes.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@faker-js/faker": "^6.0.0-alpha.6",
    "json-server": "^0.17.0"
  }
}
